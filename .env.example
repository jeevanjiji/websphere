# ==========================================
# WebSphere Environment Variables
# ==========================================
# Copy this file to backend/.env and fill in your values.
# Create frontend/.env with the VITE_ prefixed variables.
#
# NEVER commit .env files with real credentials!
# ==========================================

# ── Database ──────────────────────────────
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/websphere?retryWrites=true&w=majority&appName=Cluster0

# ── Server ────────────────────────────────
NODE_ENV=development
PORT=5000
JWT_SECRET=replace_with_a_strong_random_secret_min_32_chars
SESSION_SECRET=replace_with_another_strong_random_secret_min_32_chars

# ── CORS (comma-separated allowed origins) ─
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# ── URLs ──────────────────────────────────
BACKEND_URL=http://localhost:5000
FRONTEND_URL=http://localhost:5173

# ── Google OAuth ──────────────────────────
# Get credentials at https://console.cloud.google.com/apis/credentials
REACT_APP_GOOGLE_CLIENT_ID=your_google_client_id_here
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# ── Brevo / Sendinblue Email ──────────────
# Sign up: https://brevo.com  |  SMTP key: https://app.brevo.com/settings/keys/smtp
BREVO_API_KEY=your_brevo_smtp_api_key
BREVO_SMTP_USER=your_brevo_smtp_login
BREVO_FROM_EMAIL=noreply@yourdomain.com

# ── Push Notifications (VAPID) ────────────
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# ── Cloudinary (image/file uploads) ──────
# Dashboard: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ── Razorpay (payments) ──────────────────
# Dashboard: https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# ── Groq AI (AI Assistant + Chat Summarizer) ─
# Get key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_groq_api_key

# ==========================================
# Frontend-only variables (put in frontend/.env)
# ==========================================
# VITE_API_BASE_URL=http://localhost:5000
# VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
# VITE_VAPID_PUBLIC_KEY=your_vapid_public_key
# VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
# VITE_TURN_URL=turn:your-turn-server:3478
# VITE_TURN_USERNAME=your_turn_username
# VITE_TURN_PASSWORD=your_turn_password
# VITE_TEST_SAME_DEVICE=false
